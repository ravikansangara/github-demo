#include "f2.h"

#include <stdio.h>

void f2_first(const char *message)
{
    printf("f2_first: %s\n", message);
}

void f2_second(const char *message)
{
    printf("f2_second: %s\n", message);
}

void f2_third(const char *message)
{
    printf("f2_third: %s\n", message);
}